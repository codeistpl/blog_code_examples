 #include <network_service_config.h>

 NetworkServiceConfig:: NetworkServiceConfig(const std::string& username, const std::string& password, const std::string& address):
    username(username), password(password), address(address)
    {}